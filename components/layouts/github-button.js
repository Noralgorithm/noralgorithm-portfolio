import { Icon } from '@chakra-ui/react'
import { GoMarkGithub } from 'react-icons/go'

const GithubButton = () => {
  return <Icon aria-label='go to github' as={GoMarkGithub} boxSize={7}>
  </Icon>
}

export default GithubButton
